name = "yt-stream-proxy"
main = "worker.js"
compatibility_date = "2024-05-01"

# ── Optional: KV cache for /info responses (avoids repeat InnerTube calls)
# 1. Run: wrangler kv:namespace create CACHE
# 2. Paste the returned id below, then uncomment:
# [[kv_namespaces]]
# binding = "CACHE"
# id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
